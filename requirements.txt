# ATLAS — CPU-only. Install with: pip install -r requirements.txt
# (Tested on Python 3.12, Windows, no GPU.)
torch==2.11.0          # CPU build; on most platforms: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=5.0
safetensors
sympy
numpy
scipy
psutil
huggingface_hub
